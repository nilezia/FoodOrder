<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextLabel" parent="Widget.Design.TextInputLayout">
        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/soft_black</item>
        <item name="android:textSize">@dimen/default_text_body_large</item>
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="colorAccent">@color/linkColor</item>
        <item name="colorControlNormal">@color/linkColor</item>
        <item name="colorControlActivated">@color/linkColor</item>
        <item name="colorControlHighlight">@color/linkColor</item>
    </style>

</resources>
